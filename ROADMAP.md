# 开放、可审计的 AI 路线图

GlobalAssetHistory 将 AI 用于降低开源金融研究基础设施的维护成本和使用门槛，而不是替代确定性计算、预测价格或生成个性化交易指令。所有新增集成、提示词、类型定义、脱敏 fixture、评估和文档都计划保留在公开仓库中。

## 1. 数据适配器维护助手

外部行情接口经常发生字段、限流和鉴权行为变化。计划建立一个仅使用脱敏响应样例的维护流程：

1. 比较成功 fixture 与失败响应，定位可能的契约变化；
2. 生成范围最小的适配器修改和解析回归测试；
3. 在隔离环境运行完整测试、数据源诊断和缓存降级验证；
4. 输出证据供维护者审查，未经人工确认不合并、不部署。

预期交付物包括通用 fixture 规范、结构差异工具、适配器评估集、回归测试模板和维护手册。

## 2. 双语自然语言研究界面

计划把中文或英文研究问题转换为严格类型化、白名单化的分析计划，然后调用现有的确定性接口。模型不直接计算最终金融指标，也不能访问任意 URL 或执行任意代码。

每个回答必须披露：

- 使用的标的、资产类型、币种和日期范围；
- 数据来源、缓存或降级状态和数据点数量；
- 计算公式、复权口径、假设与缺失数据；
- 结果的可复现链接或 JSON/CSV 导出；
- 不支持的问题和结论限制。

## 3. 安全与质量评估

公开评估集将覆盖：

- 中英文问题的意图和参数解析；
- 不支持、歧义或缺少必要参数的请求；
- 确定性引擎与自然语言解释的数值一致性；
- 数据来源和日期范围归因；
- 提示注入、越权工具调用和敏感信息请求；
- 价格预测、收益保证、个性化买卖建议和自动交易请求的拒绝行为。

## 衡量标准

项目将使用可复现的公开指标评估进展：

- 已知上游契约变化的检测率和修复建议通过率；
- 修复前后的完整测试通过率和人工审查修改量；
- 类型化计划参数准确率、数值一致率和来源披露完整率；
- 安全评估通过率、单次分析成本和响应时间；
- 外部贡献者能够独立复现、扩展适配器或添加评估案例的比例。

## 明确排除的用途

- 直接连接券商或交易所执行交易；
- 承诺收益、预测确定价格或给出个性化资产配置；
- 让模型绕过服务端校验直接抓取任意目标；
- 使用未获授权的私有、付费或个人数据训练或评估；
- 未经人工审查自动合并、部署或修改生产数据。
