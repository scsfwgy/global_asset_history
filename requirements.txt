Flask==3.1.0
Flask-Cors==5.0.0
requests==2.32.3
urllib3==2.3.0
# A股龙头统计依赖（重型，仅在触发扫描时需要）
akshare>=1.16
pandas>=2.0
py-mini-racer>=0.6  # 本地 Apple Silicon 可改用 PyExecJS 替代
PyExecJS>=1.5        # py_mini_racer 的纯 Python 替代（需 Node.js）
openpyxl>=3.1
